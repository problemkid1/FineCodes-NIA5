@model IEnumerable<CRMProject.Models.MembershipType>

<div class="panel panel-primary">
    <div class="panel-heading">
        <h4 class="panel-title">Membership Type Information</h4>
    </div>
    <div class="panel-body scrollable-panel">
        <table class="table table-striped">
            <thead>
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.First().MembershipTypeName)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.First().MembershipTypeDescription)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.First().MembershipTypeFee)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.First().MembershipTypeBenefits)
                    </th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                    <tr>
                        <td>
                            <a asp-action="Details" asp-controller="MembershipType" asp-route-ID="@item.ID">
                                @Html.DisplayFor(modelItem => item.MembershipTypeName)
                            </a>
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.MembershipTypeDescription)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.MembershipTypeFee)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.MembershipTypeBenefits)
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
